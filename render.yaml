services:
  - type: web
    name: bettvplus-web
    env: python
    buildCommand: "pip install -r bettvplus-web/requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
    rootDir: bettvplus-web
